<!DOCTYPE html>
html(lang="es")
    head
        include partials/header
    body
        .contenedor-formulario.login
            h1 Crea tu cuenta en UpTask
            
            if locals.sms
                .alertas
                    each categoria in Object.keys(locals.sms)
                        each error in sms[categoria]
                            .alerta(class=`${categoria}`)
                                p= error

            form(action="/crear-cuenta", method="POST")#formulario.caja.caja-login
                .campo
                    label(for="email") E-mail
                    input(type="email" name="email" placeholder="E-mail" value= email)
                .campo
                    label(for="password") Contraseña
                    input(type="password" name="password" placeholder="Contraseña" value= password)
                .campo.enviar
                    input(type="submit", value="Crear cuenta").boton
                .campo
                    a(href="/iniciar-sesion") Iniciar Sesión
                    

        script(src="/dist/bundle.js")



