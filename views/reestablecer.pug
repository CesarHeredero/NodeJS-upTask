<!DOCTYPE html>
html(lang="en")
    head
        include partials/header
    body.login
        .contenedor-formulario
            h1= nombrePagina
            p Agrega tu e-mail para reestablecer la contraseña.

            if locals.sms
                .alertas
                    each categoria in Object.keys(locals.sms)
                        each error in sms[categoria]
                            .alerta(class=`${categoria}`)
                                p= error
                                
            form.caja-login(action="/reestablecer", method="post")
                .campo
                    label(for="email") E-mail
                    input(type="email" name="email" placeholder="E-mail" value= email)
                .campo.enviar
                    input(type="submit", value="Cambiar contraseña").boton
                .campo.acciones
                    a(href='/iniciar-sesion') Volver al inicio